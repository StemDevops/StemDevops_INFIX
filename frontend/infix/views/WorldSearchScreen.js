import { View, Text } from 'react-native';

export default WorldSearchScreen = ({ navigation }) => {
  
    return (
        <View style={{ flex: 1, alignItems: 'center', justifyContent: 'center' }}>
        <Text
          style={{ fontSize: 26, fontWeight: 'bold' }}>World Search Screen
        </Text>
      </View>
    )
  }
  