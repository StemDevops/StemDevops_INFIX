import { View, Text } from 'react-native';

export default TravelScreen = ({ navigation }) => {
  
  
    return (
        <View style={{ flex: 1, alignItems: 'center', justifyContent: 'center' }}>
        <Text
          style={{ fontSize: 26, fontWeight: 'bold' }}>Travel Screen
        </Text>
      </View>
    )
  }
  